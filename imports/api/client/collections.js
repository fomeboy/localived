import { Mongo } from 'meteor/mongo'

export const LocalLanguages = new Mongo.Collection(null)
export const LocalCountries = new Mongo.Collection(null)
export const LocalDates = new Mongo.Collection(null)
